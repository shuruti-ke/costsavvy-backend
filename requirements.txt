# Core data handling
numpy
pandas

# Environment configuration
python-dotenv

# OpenAI SDK (pinned to avoid breaking changes)
openai>=1.0.0,<2.0.0

# Web framework + server (required for deploy)
fastapi
uvicorn[standard]

# Async database access
databases
asyncpg
